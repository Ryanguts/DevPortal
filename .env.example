# Copie para .env em desenvolvimento local (NÃO suba .env no Git)
PORT=3847

# Credenciais do moderador (obrigatório em produção)
ADMIN_EMAIL=r.guts
ADMIN_PASSWORD=02022010

# Origens permitidas no CORS (separe por vírgula). Vazio = refletir origem da requisição com cuidado.
# Em produção coloque o domínio real do site, ex:
# ALLOWED_ORIGINS=https://devportal.onrender.com,https://seu-dominio.com
ALLOWED_ORIGINS=

# Ambiente
NODE_ENV=development